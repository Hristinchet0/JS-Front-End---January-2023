function SmallestOfThreeNumbers(firstNum, secondNum, thirdNum) {
    let minNumber = Math.min(firstNum, secondNum, thirdNum);

    return minNumber;
}

const smallestOfThreeNumbers = (firstNum, secondNum, thirdNum) => Math.min(firstNum, secondNum, thirdNum);